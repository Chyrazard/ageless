# Transactional email (server-only; never expose these values with NEXT_PUBLIC_)
RESEND_API_KEY=re_replace_with_a_sending_only_key
EMAIL_FROM=Ageless Evolution Website <forms@agelessevo.com>
EMAIL_RECIPIENT=hello@agelessevo.com

# Cloudflare Turnstile (server-only; the public site key is embedded in the widget)
TURNSTILE_SECRET_KEY=0x_replace_with_your_turnstile_secret
